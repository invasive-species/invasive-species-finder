import 'package:flutter/material.dart';

class ISFLoading extends StatelessWidget {
  const ISFLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
